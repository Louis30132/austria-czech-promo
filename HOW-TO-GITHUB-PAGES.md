# GitHub Pages 使用方式

這一包已經是可直接拿去做公開網站的版本。

## 你會拿到什麼

- `index.html`
- `styles.css`
- `app.js`
- `assets/`
- `.nojekyll`

## 最簡單做法

1. 到 GitHub 建一個新的 repository
2. 名稱可以先取：
   - `austria-czech-promo`
   - 或你自己喜歡的名字
3. 建好後，把這個資料夾內的所有檔案上傳到 repository 根目錄
4. 上傳完成後，到 GitHub 的：
   - `Settings`
   - `Pages`
5. 在 `Build and deployment` 裡面設定：
   - `Source` 選 `Deploy from a branch`
   - `Branch` 選 `main`
   - Folder 選 `/ (root)`
6. 按 `Save`
7. 等 1 到 3 分鐘，GitHub 會產生公開網址

## 公開網址會長什麼樣子

通常會是：

`https://你的帳號.github.io/你的-repo-名稱/`

例如：

`https://louissie.github.io/austria-czech-promo/`

## 之後要改內容怎麼辦

之後如果你要新增團型、換圖片、改文案：

1. 回來讓我幫你改
2. 我再幫你更新這一包
3. 你把新的檔案覆蓋上傳到 GitHub
4. GitHub Pages 會自動更新

## 注意

- GitHub Pages 是公開網站，知道網址的人都能看
- 不需要 GPT 帳號
- 不需要同工作區權限
- 很適合放在公司網站公告、邀請函、EDM 內文中
